export enum Status {
  ONLINE = 'Online',
  BUSY = 'Busy',
  OFFLINE = 'Offline',
  MATCH_FINDING = 'Match finding',
  IN_MATCH = 'In match',
}

export enum StatusId {
  ONLINE = 1,
  BUSY = 2,
  OFFLINE = 3,
  MATCH_FINDING = 3,
  IN_MATCH = 4,
}
