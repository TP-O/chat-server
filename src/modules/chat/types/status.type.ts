export enum Status {
  ONLINE = 'Online',
  OFFLINE = 'Offline',
  MATCH_FINDING = 'Match finding',
  IN_MATCH = 'In match',
}

export enum StatusId {
  ONLINE = 1,
  OFFLINE = 2,
  MATCH_FINDING = 3,
  IN_MATCH = 4,
}
