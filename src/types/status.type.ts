export enum Status {
  ONLINE = 'Online',
  OFFLINE = 'Offline',
  IN_ROOM = 'In room',
  MATCH_FINDING = 'Match finding',
  IN_MATCH = 'In match',
}

export enum StatusId {
  ONLINE = 1,
  OFFLINE = 2,
  IN_ROOM = 3,
  MATCH_FINDING = 4,
  IN_MATCH = 5,
}
